# Task 5.4.2

Copy the source files from Task 5.4.1 into the `src` subdirectory, then
modify the code so that `isTooLong` is an extension property rather than an
extension function.

Run the program to verify that its behaviour is unchanged.
