# lib

Kotlin Toolchain module for the image handling library needed by the
graphics application.

In a real scenario, you wouldn't have access to the source code like this;
instead, the image handling library would exist as a JAR file in an
external package repository, from which it would be downloaded as an
application dependency.
