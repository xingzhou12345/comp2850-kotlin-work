# Task 12.4.2

1. Edit `Main.kt`. Add a `main()` function that creates a `Point` object
   and then calls `println()` on that object.

   Compile and run the program. What do you see?

2. Edit `Point.kt` and override `toString()` in the `Point` class:

   ```kotlin
   override fun toString() = "($x, $y)"
   ```

3. Recompile the program and run it again. What has changed?

See the Programming Guide for further details.
