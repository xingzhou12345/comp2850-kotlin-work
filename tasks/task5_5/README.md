# Task 5.5

Copy the source files from Task 5.1.1 into the `src` subdirectory, then
modify the `anagrams()` function so that it is an infix extension function
of the `String` class. Rename the function to `anagramOf()`.

Modify the main program accordingly, then run it to check that it behaves
correctly.
