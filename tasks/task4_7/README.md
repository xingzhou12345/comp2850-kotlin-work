# Task 4.7

Write a program to find the longest line in a text file.

Your solution should go in `src/Main.kt`.

The name of the input file should be supplied on the command line. The
output generated by the program should look similar to this:

    Line 3 is the longest (length = 5)
